name: bitmap
description: Bitmap is a fast and minimalistic lib that helps you to manipulate image on Flutter apps.
version: 1.0.1
homepage: https://github.com/guptan404/bitmap
topics: [image, image-processing, ffi]

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.example.bitmap
        pluginClass: BitmapPlugin
      ios:
        pluginClass: BitmapPlugin
